"""
main.py  —  CodeBreaker Launcher
==================================
  python main.py

Starts the local game server and opens the GUI in your browser.
No installs required — pure Python standard library.
"""
from server import launch

if __name__ == "__main__":
    launch()
